name: Refresh Ticketmaster Self-Hosted

on:
  schedule:
    - cron: "47 */6 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: refresh-ticketmaster-self-hosted
  cancel-in-progress: false

jobs:
  scrape-ticketmaster:
    runs-on:
      - self-hosted
      - perth-gig-finder
      - ticketmaster
    timeout-minutes: 45
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Set up pnpm with Corepack
        run: |
          npm install --global corepack@latest
          corepack enable pnpm
          pnpm --version

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Scrape Ticketmaster AU
        env:
          SCRAPER_SOURCE_SLUGS: ticketmaster-au
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: pnpm --filter @perth-gig-finder/scraper scrape

      - name: Backfill mirrored images (best effort)
        continue-on-error: true
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: pnpm mirror-images
